import { TextBox } from "@atoms";

export default {
  title: "atoms/text-box",
  component: TextBox,
};

const Template = () => <TextBox placeholder="Add your opinion here" />;

export const TextBoxExample = Template.bind({});
